/** Link da Ouvidoria do Estado (canal de Cultura), pro botão de denúncia na
 * página do projeto. `null` até o link real ser enviado -- o botão fica
 * visível mas desabilitado enquanto isso. */
export const LINK_OUVIDORIA_CULTURA: string | null = null;

/** E-mail criado pra receber sugestões de melhoria do portal, pro botão no
 * rodapé. `null` até o e-mail ser criado -- o botão fica visível mas
 * desabilitado enquanto isso. */
export const EMAIL_SUGESTOES: string | null = null;
